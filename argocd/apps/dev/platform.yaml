apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kakamu-dev-platform
  namespace: argo
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: https://github.com/kakao4-KAKAMU/kakamu_helm.git
    targetRevision: main
    path: platform/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev-back
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
